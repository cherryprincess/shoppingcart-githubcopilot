# Codespaces/Dev Container for Shopping Cart Full Stack App
# Docs: https://containers.dev/implementors/json_reference/

# This Dockerfile is optional for extra tooling, but not required if using docker-compose.yml
FROM mcr.microsoft.com/devcontainers/typescript-node:20-bookworm

# Install global tools if needed
# RUN npm install -g <tool>

# [Optional] Install Docker CLI for Docker-in-Docker scenarios
# RUN apt-get update && apt-get install -y docker.io
